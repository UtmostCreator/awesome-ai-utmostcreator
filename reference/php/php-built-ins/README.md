# PHP Built-Ins

Supporting PHP built-in usage examples.

This directory holds built-in function and language-feature examples referenced by:

- `docs/ai/catalog.md` (`php-reference` row for `php-built-ins`)
- `.github/copilot-instructions.md`
- `justfile` (`php-builtins-search` recipe)

Add curated snippets demonstrating standard library usage and idiomatic PHP alongside this README.
