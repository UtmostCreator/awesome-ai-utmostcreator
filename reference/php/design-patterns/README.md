# PHP Design Patterns

Primary local PHP design pattern corpus for agent and human lookups.

This directory is the canonical source for design-pattern examples referenced by:

- `docs/ai/catalog.md` (`php-reference` row for `design-patterns`)
- `.github/copilot-instructions.md`
- `justfile` (`php-patterns-search` recipe)

Add curated pattern examples and brief explanations alongside this README.
