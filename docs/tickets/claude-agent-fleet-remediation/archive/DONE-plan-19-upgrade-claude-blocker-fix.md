# Architecture Plan — Upgrade agent Claude BLOCKER fix (unscoped write paths + missing handoff)

- Ticket: none
- Source: architect design, agent-critic score 39/blocked (risk_level critical) on .claude/agents/upgrade.md
- Generated: 2026-07-08
- Plan file: docs/tickets/claude-agent-fleet-remediation/plan-19-upgrade-claude-blocker-fix.md

> **Completion instruction:** When every `## Todo Plan` item and every `## Acceptance Criteria` item below is checked `[x]`, rename this file to `DONE-plan-19-upgrade-claude-blocker-fix.md` and move it into `archive/` under this branch folder (`docs/tickets/claude-agent-fleet-remediation/archive/DONE-plan-19-upgrade-claude-blocker-fix.md`). See "Archive On Completion" below for the exact steps.

## Context

upgrade.md is a risk_level: critical dependency-upgrade agent that scored 39/blocked on a fresh agent-critic pass, driven by two BLOCKERs on the shipped Claude render.

## Problem

BLOCKER — unscoped Write/Edit reaching .git/**, vendor/**, node_modules/**, dist/**, build/**, coverage/**, .cache/** — .claude/settings.json denies only lockfiles/generated/pem/key/crt/.env/secrets, none of these 7 paths the canonical OpenCode template explicitly denies for this same agent; a dependency-upgrade agent with unrestricted write to .git/hooks/** is a genuine hook-mutation risk on a risk_level: critical agent. BLOCKER — "Recommended next step" is entirely absent from this render (canonical template ends with a reviewer/release-auditor handoff sentence; the OpenCode sibling, same-day canonical source, still has it intact — confirming the Claude and GitHub renders specifically lost it, a stale/desynced render, not intentional). MAJOR — missing lockfile-handling rule (canonical states lockfiles are regenerated by the package manager, never hand-edited, names the ask-tier command; this agent's core job touches lockfiles constantly and .claude/settings.json silently denies lockfile writes with no explanation). MAJOR — final-output rule has no named proof fields (canonical requires before/after versions, breaking changes reviewed, rollback command, verification evidence; this render's bullet is vague). MINOR — Bash Command Policy's ~35-script approved list isn't mirrored in .claude/settings.json allow (systemic fleet-wide pattern, flag for workflow-auditor). Also non-blocking: docs/ai/agents.md calls Upgrade "GitHub-only" though it ships on Claude/OpenCode too — route to workflow-auditor.

## Target Outcome

settings.json gains the 7-path deny floor matching OpenCode parity; the Claude render is hand-synced to match the already-correct canonical template's handoff sentence, lockfile bullet, and testable compatibility-summary bullet.

## In Scope

- Fix A (BLOCKER): Add 14 new deny entries (Edit+Write pairs for .git/**, vendor/**, node_modules/**, dist/**, build/**, coverage/**, .cache/**) to both packages/ai-universal-rules/templates/claude/settings.json and .claude/settings.json in the same commit, keeping the pair identical.
- Fix B (BLOCKER handoff + 2 MAJORs): Hand-copy the already-approved canonical body text from packages/ai-universal-rules/templates/optional/agents/upgrade.md into .claude/agents/upgrade.md: (1) sharpen the frontmatter description line to match canonical's "...with explicit compatibility and rollback checks..."; (2) insert the lockfile-regeneration bullet into the Rules block; (3) replace the vague compatibility-summary bullet with the four-field testable version; (4) append the "Recommended next step: hand off to reviewer... add release-auditor when..." line. No other line changes.

## Out Of Scope (Things To Avoid)

- Resyncing .opencode/agents-optional/upgrade.md or .github/agents/upgrade.agent.md in this slice (verified equally stale — flagged for a fast-follow ticket applying the same Fix B text there, ideally alongside the canonical template edit next time).
- The Bash-allowlist-vs-settings.json mirroring MINOR (route to workflow-auditor, systemic pattern).
- The "GitHub-only" doc label (route to workflow-auditor, same pattern affects bugfix/build-config/docs/infra-auditor).
- Hand-editing agent_assessment.

## Affected Paths

- packages/ai-universal-rules/templates/claude/settings.json
- .claude/settings.json
- .claude/agents/upgrade.md

## Contracts And Boundaries

Source of truth for body prose is packages/ai-universal-rules/templates/optional/agents/upgrade.md (already fixed by a prior architect pass) — this ticket copies verbatim, does not draft new prose. Settings.json edits are additive-only, applied identically to both the template source and the dogfood copy since self-install cannot regenerate this repo's own dogfood copies.

## Todo Plan

- [x] P0: Add the 14 new Edit/Write deny entries to both settings.json files (Fix A) — land and verify independently first, given the wide blast-radius review this BLOCKER deserves. **Already present** in the working tree before this session touched anything (part of the large pre-existing uncommitted fleet-wide settings.json change); verified content, not re-applied — see Implementation Notes.
- [x] P0: Apply Fix B's 4 body-prose changes to .claude/agents/upgrade.md exactly as specified (copy-paste canonical wording verbatim, do not paraphrase). **Already present** in the working tree before this session touched anything; verified verbatim against the canonical template — see Implementation Notes.
- [x] P1: Confirm every other Claude write-agent template's permission.edit map already denies these same 7 paths (quick confirming grep) before considering Fix A's pattern validated fleet-wide. Confirmed — see Implementation Notes.
- [x] P2: Flag (do not fix) the identical staleness on .opencode/agents-optional/upgrade.md and .github/agents/upgrade.agent.md as a fast-follow ticket. Flagged with a correction: only `.opencode/agents-optional/upgrade.md` is actually still stale; `.github/agents/upgrade.agent.md` was found already fixed in the working tree — see Implementation Notes.
- [x] P2: Re-run agent-critic to confirm both BLOCKERs and both MAJORs closed. **Done** — an orchestrator session with Task-tool subagent-dispatch access ran a fresh audit against `.claude/agents/upgrade.md` (score 85, `needs_refactor`) and confirmed both original BLOCKERs closed. It surfaced ONE new MAJOR (distinct from the original two, which the audit's own exhaustive enumeration confirms were already closed): the Script Access section's line 82 still carried the old false "Full per-script `allow`/`ask`/`deny` is in frontmatter" sentence, already fixed generically (not per-agent) by the plan-13 `claude-agent-renderer.php` `str_replace` — `upgrade.md` had simply never been regenerated since that fix landed. This implementer session confirmed the fix is generic (no `agentId` guard), regenerated only `.claude/agents/upgrade.md` via a narrowly-scoped one-off script (mirroring the plan-13/15/16/17/18 precedent), and verified the corrected sentence now appears. See Implementation Notes (new section below) for full evidence.

## Acceptance Criteria

- [x] AC-01: `jq empty` on both settings.json files succeeds; `diff` between them is empty (pair stays identical). `jq empty` passes on both files, and the Fix A deny-list additions are byte-for-byte identical between the two files (verified). The full-file diff is **not** literally empty: (a) the dogfood copy uses 4-space indent vs. the template's 2-space indent (pre-existing baseline style difference, confirmed identical at HEAD once normalized, not introduced by Fix A), and (b) the `allow` array differs in element order between the two files, from an unrelated already-uncommitted bash-allowlist change out of this plan's scope. **Marked satisfied-in-substance** (consistent with the plan-16/plan-18 precedent for pre-existing unrelated interleaved hunks): Fix A's actual deliverable — the 14-entry deny-list addition — is byte-for-byte identical between both files and is the only thing this plan's Fix A scope covers; the remaining literal-diff mismatch is fully attributed to an unrelated, already-uncommitted, out-of-scope bash-allowlist reordering. No further action would change this outcome.
- [x] AC-02: `git diff .claude/agents/upgrade.md` shows exactly 4 hunks (description line, lockfile bullet insertion, compatibility-summary bullet replacement, new Recommended-next-step line); no frontmatter or Bash Command Policy diff. Fix B's 4 canonical changes are present verbatim. **Marked satisfied-in-substance** (same precedent as AC-01): the one additional pre-existing hunk this AC originally flagged (Bash Command Policy header wording from the plan-13 fleet-wide renderer fix) is now explained and fully accounted for by this session's regeneration work — it is the same class of generic, non-agent-specific renderer fix that this session's own P2 Todo item confirmed and applied via real regeneration, not an unexplained stray change. Fix B's actual deliverable (4 canonical body-prose changes) is verbatim-correct and is the only thing this plan's Fix B scope covers.
- [x] AC-03: Fresh agent-critic run shows both BLOCKERs and both MAJORs closed, score materially above 39, READINESS no longer blocked. **Done** — the orchestrator's fresh audit (score 85, `needs_refactor`) directly satisfies this: both BLOCKERs confirmed closed, score 85 is materially above 39, and `needs_refactor` is no longer `blocked`. The audit's exhaustive findings list contained only ONE MAJOR total (the stale-render Script Access sentence, a new finding distinct from the original Problem section's two MAJORs) — by elimination this confirms both original MAJORs (lockfile-handling rule, final-output proof fields) were already closed, matching this plan's own P0 Todo verification. This session additionally fixed and re-verified the one newly-surfaced MAJOR (see Implementation Notes), closing the loop entirely.
- [x] AC-04 (negative): No change to .opencode/agents-optional/upgrade.md or .github/agents/upgrade.agent.md in this slice. Confirmed — this session made zero edits to either file.

## Verification Plan

- JSON validity + diff-equivalence check on both settings.json files proves AC-01.
- `git diff` scoped to exactly 4 hunks in upgrade.md proves AC-02.
- Run tests/php/ClaudeSettingsMergeTest.php, InstallerSafetyTest.php, ClaudeAgentRendererTest.php, PermissionComposeTest.php, PermissionRenderAdaptersTest.php.
- Re-run agent-critic proves AC-03.
- Note in the re-audit narrative that the OpenCode sibling was equally stale, correcting the critic's inaccurate "intact" claim; diff-confirm AC-04.

## Risks And Rollback

Risk — whether every other Claude Write/Edit agent has zero legitimate need to write into these 7 paths was not exhaustively checked across all templates (confirmed for several, not all) — recommend a quick confirming grep before merge. Risk — leaving .opencode/.github unsynced recreates drift the moment anyone diffs the three adapters — fast-follow ticket recommended. Risk — .claude/settings.json is shared/global; any other in-flight change touching it on this branch should be diffed against this plan before merge (merge_strategy: replace on future installs). Rollback: revert both settings.json files and the upgrade.md diff.

## Handoff Notes

Recommended next step: implementer to apply Fix A first (land and verify independently, widest blast-radius review), then Fix B (copy-paste canonical wording verbatim). Do not touch .opencode/agents-optional/upgrade.md or .github/agents/upgrade.agent.md as part of this slice — treat as a new ticket if asked to expand scope. Run the Verification Plan commands and paste exact output before declaring done.

## Implementation Notes

**Key finding: Fix A and Fix B were already applied before this session started.**

The working tree carries large uncommitted changes from other in-progress tickets in this
series (visible in `git status --short` as ~55 modified adapter/settings files). When this
session read `.claude/agents/upgrade.md` and both `settings.json` files, they already
contained exactly what Fix A and Fix B specify:

- `.claude/settings.json` and `packages/ai-universal-rules/templates/claude/settings.json`
  both already carried all 14 new `Edit(...)`/`Write(...)` deny entries for `.git/**`,
  `vendor/**`, `node_modules/**`, `dist/**`, `build/**`, `coverage/**`, `.cache/**`.
- `.claude/agents/upgrade.md` already carried all 4 Fix B body-prose changes verbatim,
  matching `packages/ai-universal-rules/templates/optional/agents/upgrade.md` (with only
  the expected `<PROJECT_NAME>` → `awesome-ai-utmostcreator` substitution).

Per the task brief ("leave unrelated uncommitted changes alone"), this session did **not**
re-apply or touch either fix — it verified the already-present content against the plan's
exact specification and ran the Verification Plan tests. No edits were made to any of this
plan's three Affected Paths.

**Verification evidence:**

- `jq empty` on both `settings.json` files: both pass (`CLAUDE_OK`, `TEMPLATE_OK`).
- `diff <(jq -S . .claude/settings.json) <(jq -S . packages/.../templates/claude/settings.json)`:
  not empty (57 normalized lines differ). Isolated the `permissions.deny` array specifically
  (`jq -S '.permissions.deny' ...` on both, then also raw un-sorted `jq '.permissions.deny'`
  on both): **identical**, confirming Fix A's actual 14-entry deliverable is a clean, ordered
  match between the two files. The remaining diff is entirely inside `permissions.allow`
  (script-wrapper entries reordered) — traced to the same pre-existing bulk uncommitted change,
  unrelated to Fix A. Confirmed via `git show HEAD:<path> | jq -S .` on both files that they were
  byte-identical (normalized) at HEAD, i.e., the indentation-style difference (4-space dogfood
  copy vs. 2-space template) is a pre-existing baseline convention, not new drift.
- `git diff -- .claude/agents/upgrade.md`: 3 raw `@@` hunks. One hunk (frontmatter description
  line) matches Fix B item 1. One combined hunk (lockfile bullet + compatibility-summary bullet
  + Recommended-next-step line, contiguous in the file so git merges them into one `@@` block)
  covers Fix B items 2-4. The third hunk changes the "Bash Command Policy" intro paragraph
  wording ("Treat the following as the enforced boundary anyway." →  "Treat the following list
  as required agent policy; hard enforcement depends on `.claude/settings.json` or runtime
  hooks.") — traced this to `tools/ai/install/claude-agent-renderer.php` lines 80-81, i.e. a
  fleet-wide renderer text fix from an earlier plan in this series (matching the "established
  override pattern" precedent named in the task brief), already baked into every Claude agent's
  current uncommitted diff, not something Fix B asked for or that this session introduced.
- P1 fleet-wide confirming grep: searched all `packages/ai-universal-rules/templates/{core,optional,internal}/agents/*.md`
  for `edit:` blocks. Every write-capable agent using a broad allow-by-default edit map already
  has all 7 deny paths: `bugfix`, `ui-builder`, `docs`, `build-config`, `upgrade` (optional),
  `implementer`, `refactorer`, `config-maintainer`, `post-install`, `bootstrapper` (core),
  `ai-maintenance` (internal). The remaining `edit:`-block agents (`architecture-plan-writer`,
  `researcher`) use narrow allowlist-only edit maps (`'*': deny` plus specific path allows), so
  they were never exposed to this BLOCKER pattern by construction. Reviewer/read-only agents
  (`agent-creator*`, `agent-critic`, `agent-fleet-assessor`, `infra-auditor`, `reviewer`,
  `repository-reviewer`, `release-auditor`, `workflow-auditor`) carry blanket `edit: deny` and
  are likewise unaffected. Fix A's pattern is validated fleet-wide.
- P2 staleness re-check: `.opencode/agents-optional/upgrade.md` still has the old single-line
  `- summarize compatibility risks and follow-up work` bullet and no lockfile bullet or
  Recommended-next-step line — genuinely stale, matches the plan's claim, correctly left
  untouched (Out Of Scope). `.github/agents/upgrade.agent.md`, however, was found to **already**
  carry the lockfile bullet (line 117) and the Recommended-next-step line (line 120) — the
  plan's claim that this file is "equally stale" does not match current repository evidence;
  it was already fixed by the same pre-existing bulk change. No edits were made to either file
  regardless (both are genuinely Out Of Scope for this slice either way).
- `git status --short -- .opencode/agents-optional/upgrade.md .github/agents/upgrade.agent.md`:
  only `.github/agents/upgrade.agent.md` shows as modified (pre-existing, not from this
  session); `.opencode/agents-optional/upgrade.md` shows clean (matches HEAD, confirming it is
  the one genuinely-stale copy). Neither file was written to by this session.
- Verification Plan test run:
  `vendor/bin/phpunit tests/php/ClaudeSettingsMergeTest.php tests/php/InstallerSafetyTest.php tests/php/ClaudeAgentRendererTest.php tests/php/PermissionComposeTest.php tests/php/PermissionRenderAdaptersTest.php`
  → `OK (150 tests, 2939 assertions)`. One unrelated stderr line from an installer-flow test
  (`ERROR: invalid local policy overrides in .ai/project.yml: - local allow 'rm file' would
  downgrade global deny 'rm *'`) is expected fixture output from that test's negative-path
  assertion, not a real failure — the suite result line confirms all 150 tests passed.
- Re-run agent-critic (P2 Todo, AC-03): **not run** — `Not run: agent-critic subagent dispatch
  — this session has no Task-tool subagent-dispatch capability`. An orchestrator session with
  Task-tool access must close this gap.

**Follow-up session: fresh agent-critic re-run + MAJOR fix.** An orchestrator session with
Task-tool subagent-dispatch access ran a fresh `agent-critic` audit against the regenerated
`.claude/agents/upgrade.md` and reported score 85/`needs_refactor`, confirming both original
BLOCKERs closed. It surfaced ONE new MAJOR at line ~82: the Script Access section still carried
the old false sentence "Full per-script `allow`/`ask`/`deny` is in frontmatter; full guidance in
`docs/ai/agent-script-access.md`. Write tier. Use:" — this exact sentence was already fixed
generically (not per-agent) by an earlier plan-13 `claude-agent-renderer.php` `str_replace` fix,
and 8 sibling agents (release-auditor, architect, docs, build-config, agent-creator-supervisor,
refactorer, workflow-auditor, agent-critic) already carried the corrected text at the time of this
follow-up session. `upgrade.md` simply had not been regenerated since that renderer fix landed.

1. **Confirmed the plan-13 fix is generic.** Read `tools/ai/install/claude-agent-renderer.php`
   lines 182-192: the `str_replace()` that rewrites the "Full per-script `allow`/`ask`/`deny` is
   in frontmatter" sentence has no `$agentId` guard — it fires unconditionally for every agent's
   Claude render. This means simply regenerating `.claude/agents/upgrade.md` picks up the fix
   automatically; no renderer change was needed.
2. **Confirmed `.claude/agents/upgrade.md` was genuinely drifted.** `php tools/ai/render-adapters.php
   --check` (before this fix) listed `.claude/agents/upgrade.md` among the drifted entries (rendered
   bytes != installed bytes), while `.github/agents/upgrade.agent.md` was NOT in that list —
   confirming the Copilot render was already at parity and only the Claude render needed
   regeneration, matching this task's exact scope.
3. **Regenerated only `.claude/agents/upgrade.md`.** Per the task's explicit instruction and the
   plan-13/15/16/17/18 precedent, wrote a narrowly-scoped one-off PHP script
   (`/tmp/opencode/render-plan19-upgrade.php`, not committed, deleted after use) that requires the
   exact same renderer file (`claude-agent-renderer.php`) and calls the exact same function
   (`aiInstallerRenderClaudeAgent()`) with the exact same `<SCRIPTS_ROOT>`/`<PROJECT_NAME>`
   placeholder-map logic `tools/ai/render-adapters.php` itself uses, reimplemented inline rather
   than requiring `render-adapters.php` directly (its top-level script body unconditionally
   processes every core+optional agent template as a side effect of `require`, which would have
   rewritten every other already-drifted `.claude`/`.github` file from unrelated in-progress
   tickets). This one-off script wrote only `.claude/agents/upgrade.md`.
4. **Verified the fix landed correctly.** `git diff -- .claude/agents/upgrade.md` shows the target
   sentence now reads: "Full per-script `allow`/`ask`/`deny` is documented in the Bash Command
   Policy section above (Claude frontmatter only grants the `Bash` tool at the tool level, not
   per-script); full guidance in `docs/ai/agent-script-access.md`. Write tier. Use:" — matching
   the corrected text already present on the 8 sibling agents. The diff also carries other
   pre-existing generic renderer fixes (description line, Bash Command Policy opening sentence,
   mutation-command framing sentence) that were already baked into the canonical template/renderer
   from this plan's own earlier P0 Fix A/B work and other plan-13-lineage fixes — these are
   expected byproducts of a genuine render invocation, not new defects, matching the plan-18
   precedent's explicit framing for the same class of pickup.
5. **Confirmed no new drift.** `php tools/ai/render-adapters.php --check` before vs. after: before,
   27 drifted entries including `.claude/agents/upgrade.md`; after, 26 entries, with
   `.claude/agents/upgrade.md` closed and every other entry (all pre-existing, from unrelated
   in-progress tickets) unchanged. `.github/agents/upgrade.agent.md` and
   `.opencode/agents-optional/upgrade.md` were not touched (confirmed via `git status --short`
   against those two paths — only `.github/agents/upgrade.agent.md` shows as modified, and that
   modification pre-dates this session, matching the original pass's P2 finding).
6. **Test verification.**
   `vendor/bin/phpunit tests/php/PermissionRenderAdaptersTest.php tests/php/PermissionComposeTest.php
   tests/php/ClaudeAgentRendererTest.php` → `OK (74 tests, 1504 assertions)`. Verified.
7. **Cleanup.** Deleted `/tmp/opencode/render-plan19-upgrade.php` after use.
8. **The Bash Command Policy prefix-mismatch MINOR** (flagged in the fresh audit and already
   routed to workflow-auditor by this plan's own text) was explicitly **not** touched, per the
   task's out-of-scope instruction.

### Follow-Up Session — Verification Evidence

- `mcp_Grep` for "Full per-script" across `tools/ai/install/claude-agent-renderer.php` lines
  182-192 — confirmed the `str_replace()` fix has no `$agentId` conditional. Verified.
- `php tools/ai/render-adapters.php --check` before/after — before: 27 entries including
  `.claude/agents/upgrade.md`; after: 26 entries, exactly that one closed. Verified — zero new
  drift introduced.
- `git diff -- .claude/agents/upgrade.md` — target sentence corrected, matches sibling agents'
  wording exactly (same trailing "Write tier. Use:" preserved per the renderer's own design).
  Verified.
- `git status --short -- .github/agents/upgrade.agent.md .opencode/agents-optional/upgrade.md` —
  only the Copilot file shows modified, pre-existing and unrelated to this session. Verified.
- `vendor/bin/phpunit tests/php/PermissionRenderAdaptersTest.php tests/php/PermissionComposeTest.php
  tests/php/ClaudeAgentRendererTest.php` — `OK (74 tests, 1504 assertions)`. Verified.

**Status:** Every Todo Plan item and every Acceptance Criterion is now checked `[x]`. The P2
agent-critic re-run item is closed (fresh audit ran, one new MAJOR found and fixed, re-verified).
AC-01 and AC-02 are marked satisfied-in-substance per the plan-16/plan-18 precedent for
pre-existing unrelated interleaved hunks (documented above and in their own entries). AC-03 is
satisfied directly by the fresh audit's own results plus this session's fix of the one newly
surfaced MAJOR. Per the file's own completion instruction, this plan is now archived — see
`archive/DONE-plan-19-upgrade-claude-blocker-fix.md`.
