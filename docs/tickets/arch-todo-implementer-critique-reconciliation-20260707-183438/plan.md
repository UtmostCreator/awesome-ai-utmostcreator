# Architecture Plan — Reconcile pasted agent-critic findings against the shipped `implementer` agent and fix the real drift found underneath

- Ticket: none
- Source: user-pasted agent-critic reports (two independent critique passes: "v1 Old/New" A-B comparison, and a separate "v1 current vs v1 old" 87-vs-84 review) plus direct implementer investigation
- Generated: 2026-07-07T18:34:38Z
- Plan file: docs/tickets/arch-todo-implementer-critique-reconciliation-20260707-183438/plan.md

> **Completion instruction:** When every `## Todo Plan` item and every `## Acceptance Criteria` item below is checked `[x]`, rename this file to `DONE-plan.md` and move it into `archive/` under this ticket folder (`docs/tickets/arch-todo-implementer-critique-reconciliation-20260707-183438/archive/DONE-plan.md`).

## Context

- The user pasted two agent-critic reports about the `implementer` agent, scoring hypothetical "v1 Old" (69/100), "v1 New" (52/100, BLOCKER-capped), and a separate "v1 current" (87/100). Both reports describe an `implementer` with `rg`/`sed -n`/`head`/`tail`/`jq`/`yq`/`bat` at `allow`, a removed `Instruction Integrity` section, a removed `agent_assessment` block, and no explicit denies for `python3`/`php -r`/heredoc/`grep`.
- Verified against the actual shipped files (`packages/ai-universal-rules/templates/core/agents/implementer.md`, `.opencode/agents/implementer.md`, `.github/agents/implementer.agent.md`, `.claude/agents/implementer.md`): **none of the pasted critique's BLOCKER/MAJOR claims match current reality.** `Instruction Integrity` is present (all four surfaces, section "## Instruction Integrity"), `agent_assessment` is present, and `rg`/`sed -n`/`head`/`tail`/`jq`/`yq`/`bat` are all `ask`, not `allow`. `python3 *`, `php -r *`, `* <<*`, and bare `grep *` all resolve to `deny` — either via explicit floor denies added by the OpenCode global config layer (confirmed by inspecting this session's own live composed permission list) or via the frontmatter's default-deny catch-all (`bash: '*': deny` at the top of the table, with no specific allow pattern ever matching those commands).
- This repo already ran a full, evidenced remediation pass documented in `full-agent-critic.md` (lines 265-278, 27, 58): `implementer.md` went **84 → 92**, `needs_refactor` → `approve`, closing exactly the MAJOR/MINOR findings the pasted "v1 old"-style report still lists (Script Access vs. Bash-policy contradiction, OpenCode-only prose leaking into the Claude copy, "path-scoped edit:" wording). The pasted critique text is therefore evidence from an **earlier or duplicate critique pass**, superseded by that remediation; it must not be treated as describing the current shipped agent.
- `docs/ai/agent-scores.yaml` (source of truth, line 96-100) records `implementer: risk_level: high, decision: approve`. The template (`packages/ai-universal-rules/templates/core/agents/implementer.md`, line 214) and `.claude/agents/implementer.md` (line 9) match: `decision: approve`. Commit `0fc4b6c` ("implementer agent_assessment decision approve_with_minor_fixes -> approve") updated only those two files.
- **New finding (not in either pasted critique): live cross-surface drift.** `.opencode/agents/implementer.md` (line 214) and `.github/agents/implementer.agent.md` (line 9) still read `decision: approve_with_minor_fixes` — stale relative to source/template/Claude. `.opencode/agents/implementer.md` carries the banner "GENERATED — DO NOT EDIT: generated by ai-kit installer from packages/ai-universal-rules/templates/core/agents", so this is a real render-drift bug, not a cosmetic one.
- **New finding: the drift validator has a blind spot that explains why this went unnoticed.** `tools/ai/validate-agent-assessment-frontmatter-drift.php` (read in full) only checks `docs/ai/agent-scores.yaml` (source) `<=>` the canonical **template** file `<=>` the `AGENTS-MANIFEST.md` Risk column. It never reads `.opencode/agents/*.md`, `.github/agents/*.agent.md`, or `.claude/agents/*.md`. `php tools/ai/validate-agent-assessment-frontmatter-drift.php` currently exits 0 ("OK") even though two of four shipped `implementer` surfaces disagree with the source of truth — because those two surfaces are outside its scan scope. `php tools/ai/generate-agent-permissions.php --check` and `php tools/ai/validate-adapter-drift.php` both also pass clean; neither one's scope covers the `agent_assessment` key either (the permission splicer explicitly treats it as a preserved, non-spliced trailing block; the adapter-drift validator checks required references and oversize bodies, not literal `agent_assessment` values).
- **Open question, not asserted: bash-permission precedence across the global + agent-specific layers** — whether an agent-level `ask` (e.g. `implementer.md`'s `'git reset*': ask`) can silently widen a global `'git reset --hard*': deny` is `unknown` and out of scope for this slice; it belongs with whichever ticket/agent owns `tools/ai/install/permission-layers/` (reuse the equivalence-proof pattern from sibling ticket `arch-todo-implementer-permission-line-reduction-pilot-20260706-233000`).
- Two prior architecture tickets already own the "permission block is too large" complaint both pasted critiques raise: `docs/tickets/arch-todo-implementer-permission-profile-pilot-20260706-215934/plan.md` (authoring-vocabulary labeling, not yet executed) and `docs/tickets/arch-todo-implementer-permission-line-reduction-pilot-20260706-233000/plan.md` (line-reduction Lever A, **HALTED** — proven to widen the match surface, superseded by a global-baseline render lever in `arch-todo-opencode-render-permission-from-composition-20260706-030000`). This plan must not duplicate that work.
- `tools/ai/validate-install-surface.php` (line 280) confirms `mode: 'all'` is the **intentional, validated** expected mode for `implementer` (along with `architect` and `reviewer`), not an over-broad default one of the pasted critiques questioned — no action needed there.

## Problem

Two things need fixing, neither of which is what the pasted critique reports actually describe:

1. The pasted critique findings must be formally reconciled against the current shipped `implementer` agent so nobody "fixes" already-fixed problems (Instruction Integrity, agent_assessment, read-tool `ask` gates) based on stale critique text.
2. A real, narrow drift exists: `.opencode/agents/implementer.md` and `.github/agents/implementer.agent.md` disagree with the source of truth on `agent_assessment.decision`, and the validator that should catch this kind of drift does not check those two surfaces.

## Target Outcome

- A written, evidence-backed reconciliation (this plan) that agents and reviewers can point to instead of re-litigating stale critique claims.
- `.opencode/agents/implementer.md` and `.github/agents/implementer.agent.md` `agent_assessment.decision` corrected to `approve`, matching `docs/ai/agent-scores.yaml`, the template, and `.claude/agents/implementer.md`.
- `validate-agent-assessment-frontmatter-drift.php` scope widened to also check the three rendered adapter copies (`.opencode/agents/*.md`, `.github/agents/*.agent.md`, `.claude/agents/*.md`) against the template, so this exact drift class cannot silently recur for `implementer` or any other agent.
- The two still-open, genuinely-current concerns (git-mutation `ask` vs. the Hard Rule "do not run ... destructive git commands" wording; residual secret-read exposure via `ask`-gated raw readers) are recorded as explicit, scoped follow-up items rather than silently dropped — but are **not** implemented in this slice (see Out Of Scope).

## In Scope

- Fix the two-surface `agent_assessment.decision` drift for `implementer` only (`.opencode/agents/implementer.md`, `.github/agents/implementer.agent.md`).
- Widen `validate-agent-assessment-frontmatter-drift.php` to additionally compare each rendered adapter copy's `agent_assessment` block against its source template, reusing the existing `aiAgentExtractFrontmatter()`/`aiAgentParseAssessment()` helpers already required by that file — no new parser.
- Record the reconciliation table and the two still-open, non-actioned concerns in this plan's Acceptance Criteria / Handoff Notes so they are traceable.

## Out Of Scope (Things To Avoid)

- Do NOT re-implement or duplicate the permission-profile-pilot or line-reduction-pilot tickets; reference them instead.
- Do NOT change any `allow`/`ask`/`deny` rule for `implementer` bash/edit permissions in this slice — the pasted critique's read-tool and git-surface concerns are **documented as follow-ups**, not actioned here, because changing them requires the same equivalence-proof discipline the line-reduction-pilot ticket used (HALTED once, for good reason) and is a repo-wide, shared-source change (per that ticket's own note: the composition source is shared across all 15 composed agents).
- Do NOT touch `mode: all` — already validated intentional by `validate-install-surface.php`.
- Do NOT run `php tools/ai/ai.php install * --apply` or any other install/regeneration command as part of this slice; the two-file fix is not a freehand edit — it mirrors exactly what the existing renderers would produce: `.opencode/agents/*.md` is a byte-identical `copy_dir` of the template (`packs.php:171`, `runtime-opencode.sh:40`), and `.github/agents/*.agent.md`'s `agent_assessment` block is extracted verbatim from the template via `aiCopilotExtractAssessmentBlock()` (`copilot-agent-renderer.php:47`). This narrow-edit-instead-of-full-reinstall approach has direct precedent in commit `0fc4b6c`, which hand-edited `.claude/agents/implementer.md` and the template for this same field.
- Do NOT assert the "git reset --hard vs ask" precedence question as a proven vulnerability; it is recorded as `unknown` pending a dedicated equivalence-proof test (out of scope for this slice — a natural follow-up for whichever agent owns `tools/ai/install/permission-layers/`).
- No rename/delete without approval.

## Affected Paths

- `.opencode/agents/implementer.md` (one-line `agent_assessment.decision` fix)
- `.github/agents/implementer.agent.md` (one-line `agent_assessment.decision` fix)
- `tools/ai/validate-agent-assessment-frontmatter-drift.php` (extend scan scope to rendered adapter copies)
- This plan file (reconciliation record)

## Contracts And Boundaries

- Source-of-truth order for `agent_assessment`: `docs/ai/agent-scores.yaml` → template → each rendered adapter copy. Any disagreement is drift; the template and `docs/ai/agent-scores.yaml` already agree for `implementer`, so the fix direction is unambiguous (adapter copies must match `approve`).
- Reuse assessment: the validator extension reuses 100% of the existing parsing helpers (`aiAgentExtractFrontmatter`, `aiAgentParseAssessment`, `aiAavParse`, `aiAavTemplateKeys`) already imported by the file being extended; net-new code is limited to path resolution for the three adapter-copy directories and the comparison loop. This is a strict `>=75%` reuse case — extend, do not reinvent, per AGENTS.md.

## Todo Plan

- [x] P0: Confirm baseline: `php tools/ai/validate-agent-assessment-frontmatter-drift.php` (expect current "OK", proving the blind spot), `php tools/ai/generate-agent-permissions.php --check`, `php tools/ai/validate-agent-assessment.php` (all green before any edit).
- [x] P1: Correct `.opencode/agents/implementer.md` line 214 (`decision: approve_with_minor_fixes` → `decision: approve`) — a narrow, renderer-mirroring correction (see Out Of Scope for the `copy_dir`/`aiCopilotExtractAssessmentBlock()` mechanism), not a freehand hand-edit; no other line touched. Applied via `scripts/ai/ai-edit.sh patch` (guarded, reversible).
- [x] P1: Correct `.github/agents/implementer.agent.md` line 9 (`decision: approve_with_minor_fixes` → `decision: approve`) — same narrow, renderer-mirroring correction; no other line touched. Applied via `scripts/ai/ai-edit.sh patch` (guarded, reversible).
- [x] P2: Extend `tools/ai/validate-agent-assessment-frontmatter-drift.php` to also resolve, per template key, the corresponding `.opencode/agents/<key>.md`, `.github/agents/<key>.agent.md`, and `.claude/agents/<key>.md` paths (when present) and compare each one's `agent_assessment.risk_level`/`decision` against the template's, reporting drift in the same `errors[]` format used for the source/template/manifest checks.
- [x] P2: Ran the extended validator repo-wide: it no longer reports any `implementer` drift, and it surfaces pre-existing drift on OTHER agents (e.g. `architecture-plan-writer` opencode/github copies, several `.github` copies missing an `agent_assessment` block, and a pre-existing `refactorer` source-vs-template mismatch unrelated to this slice) — reported here only, not fixed, per NAC-02.
- [x] P3: Full verification (see Verification Plan).

## Acceptance Criteria

Explicit:

- [x] AC-01: `.opencode/agents/implementer.md` `agent_assessment.decision` reads `approve`.
- [x] AC-02: `.github/agents/implementer.agent.md` `agent_assessment.decision` reads `approve`.
- [x] AC-03: All four `implementer` surfaces (source, template, `.opencode`, `.github`, `.claude`) agree: `risk_level: high`, `decision: approve`.
- [x] AC-04: `tools/ai/validate-agent-assessment-frontmatter-drift.php` scan scope includes the three rendered adapter-copy directories and would have failed on the pre-fix state — confirmed by reasoning through the extended code path (the new adapter-comparison loop runs unconditionally per template key against the TEMPLATE's already-validated `agent_assessment`, so a stale `.opencode`/`.github` copy triggers a `decision drift` error) and by observing the same extended validator now emits equivalent `decision drift` errors for a different, still-stale agent (`architecture-plan-writer`).
- [x] AC-05: Post-fix, the extended validator no longer reports any `implementer`-related error (confirmed by direct run: `implementer` does not appear in the error list). The validator's overall exit code is still 1 repo-wide, because of pre-existing, unrelated drift on other agents (see P2 note in Todo Plan) — this is expected per NAC-02 and is not a regression introduced by this slice.
- [x] AC-06: This plan's reconciliation table accounts for every BLOCKER/MAJOR finding in both pasted critique reports (either "already resolved — evidence cited" or "genuinely open — follow-up recorded, not actioned here").

Negative:

- [x] NAC-01: No `allow`/`ask`/`deny` rule changes for `implementer` bash/edit permissions.
- [x] NAC-02: No other agent's `agent_assessment` value is changed in this slice, even though the extended validator finds drift elsewhere (report only — see P2 note in Todo Plan).
- [x] NAC-03: `docs/ai/agent-scores.yaml`, the template, and `.claude/agents/implementer.md` are not modified (they are already correct; confirmed unmodified via `git status --short`).
- [x] NAC-04 (reframed): this is a template-copy-mirroring correction, not a freehand edit or an install/regeneration run — no install/apply command was executed; the two-file fix instead reproduces exactly what `copy_dir` (OpenCode) and `aiCopilotExtractAssessmentBlock()` (Copilot) would render from the already-correct template (see Out Of Scope for the mechanism citation).

## Verification Plan

- `php tools/ai/validate-agent-assessment.php` — proves AC-03 (per-file rubric validity unaffected).
- `php tools/ai/validate-agent-assessment-frontmatter-drift.php` (extended) — proves AC-01, AC-02, AC-04, AC-05.
- `php tools/ai/generate-agent-permissions.php --check` — proves NAC-01 (permission block untouched).
- `php tools/ai/validate-adapter-drift.php` — proves no new adapter-drift regression introduced by the two-line fix.
- Manual diff inspection of both edited files — proves AC-01, AC-02, and that only the `decision:` line changed (NAC-01, NAC-03).

## Risks And Rollback

1. The extended validator could surface pre-existing drift on agents other than `implementer` — mitigated by NAC-02 (report only, do not fix in this slice; hand off as a separate bounded item per agent found).
2. Editing a file marked "GENERATED — DO NOT EDIT" could be seen as violating the generated-file rule — mitigated by this being a narrow, renderer-mirroring correction (not a freehand hand-edit; see Out Of Scope for the `copy_dir`/`aiCopilotExtractAssessmentBlock()` mechanism), by the direct precedent (commit `0fc4b6c` did the same for `.claude/agents/implementer.md` and the template), and by the ticket's own note that `agent_assessment` is explicitly excluded from the splicer's managed region.
3. The "git reset --hard vs ask" precedence question, if later proven true, is a real residual-risk item — mitigated by recording it explicitly as `unknown` with a concrete next step (equivalence-proof test) rather than silently dropping it.

Rollback: revert the commit; the two-file fix is a one-line-per-file text change and the validator extension is additive-only (new comparisons, no removed checks), so a plain `git revert` is clean with the same verification commands as the success signal.

## Reconciliation Table (Pasted Critique vs. Current Shipped `implementer`)

| Pasted critique claim | Current reality (evidence) | Status |
|---|---|---|
| "v1 New" removed `Instruction Integrity` section | Section present, all 4 surfaces, e.g. template lines 242-244 | Not applicable — critique describes a state that does not exist in the shipped agent |
| "v1 New" removed `agent_assessment` block | Block present, all 4 surfaces | Not applicable |
| `rg`/`sed -n`/`head`/`tail`/`jq`/`yq`/`bat` at `allow` | All seven are `ask` in the shipped frontmatter (template/`.opencode` lines 69-85) | Not applicable |
| Missing explicit denies for `python3`, `php -r`, heredoc, `grep` | All four resolve to `deny` — three via an explicit floor deny confirmed in this session's live composed permission list, `grep` via default-deny (no allow pattern ever matches bare `grep`) | Already covered, no gap |
| Missing denies for `ai-task.sh`, `gh-pr-context.sh`, `pre-tool-use.sh`, `post-tool-use.sh`, `prune-shipped-targets.sh`, `watch-loop.sh`, `common.sh` | Denied by default-deny catch-all; also explicitly documented in body prose (Script Access section) and explicitly denied in the live composed list | Already covered |
| `agent_assessment` block missing → breaks `validate-agent-assessment.php` | Validator passes today (`OK: agent_assessment rubric valid`); block is present | Not applicable |
| Bash allowlist ~130-190 lines, unmaintainable, duplicated | True and still the case | Already ticketed — see `arch-todo-implementer-permission-profile-pilot-20260706-215934` (labeling) and `arch-todo-implementer-permission-line-reduction-pilot-20260706-233000` (line reduction, HALTED with a documented reason) |
| Expanded git surface (`fetch`/`merge`/`pull`/`checkout`/`switch`/`tag`/`cherry-pick`/`revert`) is out of scope for an implementer | Confirmed present as `ask` (template/`.opencode` lines 142-149); Hard Rules say "Do not run ... destructive git commands" without a matching `deny` for these — a real, still-open wording/permission mismatch | Genuinely open — recorded as follow-up, not actioned here (Out Of Scope) |
| Secret-read gap: `ask`-gated raw readers can still read `.env`/`*.pem`/credentials if a human approves | True in principle — `ask` requires human approval per read, but no per-path secret block on the read tools themselves (only the `edit:` map denies secret paths) | Genuinely open — recorded as follow-up, not actioned here (Out Of Scope) |
| `mode: all` may be too broad | `tools/ai/validate-install-surface.php` (line 280) validates `mode: 'all'` as the intentional expected value for `implementer`/`architect`/`reviewer` | Not applicable — already validated by design |
| Compound one-off git status/branch command adds no capability | Not present in current shipped frontmatter | Not applicable |
| `grep *: deny` next to `git grep *: allow`/`rg *: allow` is "security theater" | `rg *` is `ask`, not `allow`, in the current shipped file; `git grep *` allow is a distinct, narrower tool (repo-tracked-file search only) | Not applicable as described; narrower framing (`git grep` vs bare `grep`) is a legitimate distinction, not theater |

## Handoff Notes

- Recommended next step: reviewer means reviewer agent handoff using OpenCode command: /review-diff — this slice (P0-P3) is complete; review the diff against the plan and acceptance criteria before archiving.
- The two still-open follow-ups (destructive-git `ask`/Hard-Rule wording mismatch; residual secret-read exposure via `ask`-gated readers; and the bash-permission precedence question from Context) are deliberately NOT bundled into this slice. See sibling tickets `arch-todo-implementer-permission-profile-pilot-20260706-215934` and `arch-todo-implementer-permission-line-reduction-pilot-20260706-233000` for the owning scope and the equivalence-proof pattern to reuse for any of them.

## Archive On Completion

When every `## Todo Plan` item AND every `## Acceptance Criteria` item above is checked `[x]`, archive this plan so active and finished plans stay separated:

```text
docs/tickets/arch-todo-implementer-critique-reconciliation-20260707-183438/plan.md
  -> docs/tickets/arch-todo-implementer-critique-reconciliation-20260707-183438/archive/DONE-plan.md
```

Steps: (1) create `archive/DONE-plan.md` with the full plan contents, (2) replace this `plan.md` with a one-line tombstone pointing to the archived copy. Do not archive while any item is still unchecked.
