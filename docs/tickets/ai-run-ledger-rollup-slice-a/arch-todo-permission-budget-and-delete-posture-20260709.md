# Permission budget raise + external-read & delete-to-ASK posture (governance remediation)

- Status: PARTIALLY COMMITTED — P0 + delete-marker doc rule committed; P1 (compositions.php/packs.php) implemented and verified but held back from commit due to entanglement with unrelated pre-existing content (see Implementation Notes "Entanglement Finding")
- Branch: ai-run-ledger-rollup-slice-a
- Risk: medium (touches approval boundaries)
- Primary verification: `composer test` / `php tools/ai/validate-install-surface.php`
- Plan file: docs/tickets/ai-run-ledger-rollup-slice-a/arch-todo-permission-budget-and-delete-posture-20260709.md

> **Completion instruction:** When every `## Todo Plan` item and every `## Acceptance Criteria` item below is checked `[x]`, rename this file to `DONE-arch-todo-permission-budget-and-delete-posture-20260709.md` and move it into `archive/` under this branch folder (`docs/tickets/ai-run-ledger-rollup-slice-a/archive/DONE-arch-todo-permission-budget-and-delete-posture-20260709.md`).

## Summary

This is a small correction of an over-scoped prior external plan (`analyzed-log-failed-commands-v1.md`) that proposed a 30-item rewrite built on `unknown` assumptions. Read-only grounding by the researcher and architect confirmed that MOST of that scope ALREADY EXISTS and must NOT be recreated. Overlap with the existing permission-layers compiler, rendered adapters, and install-surface validator is >75%, so this remediation is roughly 3 workstreams (not 30) and recreates nothing. Only the genuinely verified gaps are addressed: raising two agent-file line budgets, expressing external-read + delete-to-ASK posture through the existing composition, and one optional simulation test.

## In Scope

- Raise the two agent-file line budgets (`opencode-agents`, `package-agent-templates`) in `packages/ai-universal-rules/policies/ai-file-standards.json`.
- Express external-read + delete-to-ASK posture THROUGH the existing permission-layers composition and its rendered adapters (not a new system).
- (Optional P2) Add the one genuinely-missing permission-policy simulation test.
- (Plan-only, forward-looking) Note the P9 shared-role-prose extraction path.

## Out Of Scope (Things To Avoid)

- The 187-file pre-existing `claude-agent-fleet-remediation` diff — MUST NOT be staged, reverted, or modified by this work.
- Product / application code.
- Host reconfiguration — do NOT weaken the OpenCode host `external_directory: ask` to a blanket allow at the host level.
- Re-architecting the permission-layers system, the permission compiler, or the install-surface validator.
- Inventing a `.claude/agents/*.md` line budget (no such rule exists in this policy, and `validate-adapter-drift.php` does not measure body size — do not add one).
- Recreating the already-shipped secret-read deny backstop or destructive-command deny pack.

## Grounding Facts (already verified — evidence, not to re-verify)

- Branch: `ai-run-ledger-rollup-slice-a`. Worktree ALREADY has 187 modified files from unrelated `claude-agent-fleet-remediation` work.
- Line-budget policy: `packages/ai-universal-rules/policies/ai-file-standards.json`. Two agent-file rules currently sit at `warn_above: 240`, `fail_above: 320`:
  - `opencode-agents` rule (JSON lines ~110-129; patterns `.opencode/agents/*.md`)
  - `package-agent-templates` rule (JSON lines ~192-223; patterns `packages/ai-universal-rules/templates/core/agents/*.md`)
  - Both already carry `warn_allowlist` entries referencing the "P9" shared-role-prose extraction path.
- `.claude/agents/*.md` have NO line-budget rule in this policy at all, and `tools/ai/validate-adapter-drift.php` does not measure body size.
- Permission source-of-truth + compiler ALL EXIST and must be reused, not recreated: `tools/ai/install/permission-layers/` (script-tiers.php, rules.php, patterns.php, compositions.php, compose.php, render-adapters.php, packs.php, edit-surfaces.php, verify-tiers.php, render-spec.php, core.php, agent-spec.php), plus `tools/ai/compile-command-policy.php`, `tools/ai/generate-agent-permissions.php`, `tools/ai/render-agent-permissions.php`, `tools/ai/render-adapters.php`, `tools/ai/generate-claude-settings.php`.
- `tools/ai/validate-install-surface.php` (909 lines) EXISTS and enforces line budgets from the policy.
- Secret-read deny backstop and destructive-command deny pack already shipped.
- `external_directory` posture is owned by `render-spec.php` (sets `external_directory` maps, e.g. `allow`/nested), rendered by `render-adapters.php` (`aiPermissionRenderAdapters()` emits the nested `external_directory:` YAML block); compositions consume it via `compositions.php`. Existing OpenCode host config uses `external_directory: ask`.
- `edit-surfaces.php` defines named edit surfaces (`none`, `unrestricted`, `tickets`, `code`, `docs`, `config`, `ui`, `install`, `research-sessions`) with a shared `$denyTail` of secret/lockfile/generated denies. There is NO explicit named "deletion" or `rm` edit surface here — file-deletion posture for bash `rm` is governed by the bash script-tiers/rules layer, NOT by edit-surfaces.
- `.claude/settings.json` EXISTS (rendered). Rendered OpenCode permission block + `.claude/settings.json` are the adapter outputs.
- No `PermissionPolicy*Test.php` currently exists.
- REVIEWER-VERIFIED (ground truth, read-only against repo):
  - External read posture is NOT uniformly `ask` today — it is PER-AGENT and heterogeneous. `tools/ai/install/permission-layers/render-spec.php` line 43 gives `script-runner` `external_directory => 'allow'`; lines 78-81 give `architecture-plan-writer` a path-scoped `external_directory` `allow` for docs/tickets. Goal = per-agent external READ access with `ask` as the SAFE DEFAULT for agents that currently have none (NOT "keep everyone at ask").
  - `.claude/settings.json` is a SINGLE GLOBAL file, not per-agent. It has only `allow` and `deny` arrays (NO `ask` array). Its global deny includes `"Bash(rm -rf *)"` (line 126). Consequence: per-agent "delete = ASK for implementer/super-implementer only" is NOT expressible on the Claude surface (global-or-nothing). Per-agent delete/ask tiers ARE expressible ONLY in the OpenCode permission block (permission-layers renders per-agent).
  - `rm` is NOT tiered in `patterns.php`/`rules.php`/`script-tiers.php` — it is emitted as a literal string into the rendered deny lists. Implementer must FIRST trace what emits `"Bash(rm -rf *)"` into the deny list (composition -> render path) before editing; the owner-file candidate list is a guess to be confirmed by tracing.
  - On Claude, installs are already NOT hard-denied in `settings.json` (no install deny present), so anything not in allow/deny falls through to the runtime prompt = effectively ask. The permission-layers system DOES have an `ask` tier mechanism: `rules.php` line 41 `aiPermissionRule('bash', $pattern, 'ask')` and `script-tiers.php` `'ask'` tiers (`ai-write`, `ai-context-ask`, `ai-verify`).

## Affected Paths

- `packages/ai-universal-rules/policies/ai-file-standards.json` (P0 budget edit)
- `tools/ai/install/permission-layers/render-spec.php` (P1 external-read posture)
- `tools/ai/install/permission-layers/render-adapters.php` (P1 render)
- `tools/ai/install/permission-layers/compositions.php` (P1 per-agent wiring, impl/super-impl specs)
- `tools/ai/install/permission-layers/script-tiers.php` (P1 bash `rm` tier — candidate)
- `tools/ai/install/permission-layers/rules.php` (P1 — candidate)
- Rendered outputs: `.claude/settings.json`, OpenCode permission block (regenerated, not hand-edited)
- (P2, optional) `tests/.../PermissionPolicy*Test.php` (new)

## Contracts And Boundaries

- Permission source-of-truth is the `permission-layers/` composition; adapters (`.claude/settings.json`, OpenCode block) are RENDERED, not authored by hand.
- Approval-boundary changes (delete posture, external access) are gated per AGENTS.md.
- Cross-runtime nuance: OpenCode has native permission enforcement; Copilot is guidance-only (hardest surface). Budgets are the safe lever, NOT permission-count reduction.

## Todo Plan

### P0 — Raise agent line budgets (low risk)

- [x] P0: Edit `packages/ai-universal-rules/policies/ai-file-standards.json` — for the `opencode-agents` rule and the `package-agent-templates` rule, change `warn_above` 240 -> 275 and `fail_above` 320 -> a single value in 340-360 (recommend 350). Rationale: instructions must be ADDABLE without REMOVING instructions. Do NOT touch the other rules. Preserve existing `warn_allowlist`/`fail_allowlist` entries. **DONE**: both rules set to `warn_above: 275`, `fail_above: 350`; no other rules touched; allowlists preserved verbatim. Verified via `php tools/ai/validate-install-surface.php` (exit 0, "OK: install surface validation passed").

### P1 — External-read + delete-to-ASK posture via existing composition (MEDIUM risk — approval-boundary change)

- [x] P1: Sub-item A (external read — PER-AGENT, `ask` default) — **VERIFIED ALREADY SATISFIED, no code change**. Deeper trace during implementation found `opencode.jsonc:226` and `packages/ai-universal-rules/templates/core/opencode.json:226` set a GLOBAL project-level default `"external_directory": "ask"` that every agent inherits unless it explicitly overrides via a per-agent `external_directory` map. This is test-enforced: `tests/php/AgentPermissionPolicyTest.php::testProjectConfigAsksForExternalDirectoryAndLoadsBoundaryDocs` (line 410) asserts `$config['permission']['external_directory'] === 'ask'` for both project config files. Only `script-runner` (`allow`) and `architecture-plan-writer` (path-scoped `allow` for `docs/tickets/**`) intentionally loosen this, both already reviewed/tested. No agent has a hard-deny or silent-none on external read; the "ask default for agents with none" goal was already true before this slice. Corrects the plan's own earlier framing (which had not yet located the global config default).
- [x] P1: Sub-item B (delete-to-ASK — split by runtime surface) — **DONE on OpenCode; documented limitation on Claude**. Traced the true owner: `rm -rf *` is on the TRUE immutable hard-deny floor (`core.php` line 30, `hard-deny` layer, enforced by `aiPermissionAssertNoHardDenyWeakening()` in `compose.php` — no agent may loosen it; left untouched). Plain `rm *` (distinct pattern, not on the immutable floor) was the real gap: `implementer` had no override so it fell through to its `deny` `shipped_star_baseline`; `super-implementer` had no override so it fell through to its `allow` `shipped_star_baseline` (silently allowed, zero confirmation — the opposite of the intended posture). Added a new shared pack `hard_stop.ask_rm` (`rm *` => `ask`) in `packs.php` and wired it via `askPacks` for `implementer`, `super-implementer`, and `post-install` (which already had an equivalent inline exception — consolidated to avoid the DRY-duplication test failure, see Implementation Notes). Claude `.claude/settings.json` is a single GLOBAL file with only `allow`/`deny` arrays (no `ask`) and hard-denies `"Bash(rm -rf *)"` globally (line 126) — per-agent delete=ASK is genuinely not expressible there; documented as a cross-runtime limitation, not implemented (would require inventing new Claude tooling, out of this slice's scope).
- [x] P1: Sub-item C (all agents can ASK to read + execute read-only) — **VERIFIED ALREADY SATISFIED, no code change**. Raw `grep`/`find`/`cat`/`sed`/`awk` denial (where present) is a deliberate script-first design, not an accidental hard-block: the governed wrapper tier (`ai-search.sh`, `preview-file.sh`, `query-usage.sh`, `rg-code.sh`, `fd-files.sh` — `script-tiers.php` line 57) is allow/ask for effectively all agents and is the documented, redaction-aware read-only-execution surface (`docs/ai/tools/ai-search.md`: "Prefer it over raw grep, find"). Converting raw-tool denials to ask would contradict this established policy and weaken the redaction benefit of routing reads through `preview-file.sh`. Every agent has an allow-or-ask path to read-only execution through the wrapper tier; none is left with zero read capability.
- [x] P1: Sub-item D (edit-capable agents can ASK to edit outside root + ASK for command exec) — **VERIFIED ALREADY SATISFIED, no code change**. "Ask to edit outside root" is the same global `external_directory: ask` default confirmed in Sub-item A, which applies to edit-capable and read-only agents alike. "Ask for command execution" is already true for every known-risky-but-legitimate command category (installs via `core:package-manager-ask`, git mutations via `core:git-mutating-ask`, deletion via the new `hard_stop.ask_rm` pack); the remaining default-deny is scoped to the immutable floor plus genuinely-unbounded/arbitrary commands (the `'*'` catch-all), which is an intentional, deliberate safe-default boundary (prevents unrestricted arbitrary code execution) and was NOT weakened — broadening the catch-all itself was judged out of proportion to the user's actual request and a real safety regression, so it was not done.
- [x] P1: Sub-item E (installs = warn-then-ASK, no hard deny) — **DONE**. Found the real gap: `refactorer` and `docs` denied the FULL 11-pattern package-manager set via the `package_manager.deny_all_mutations` pack; `bugfix`/`build-config`/`upgrade` denied 4 patterns (`pnpm add*`, `yarn add*`, `bun install*`, `bun add*`) via `package_manager.narrow_no_add_or_bun_deny`. Both packs' entries flipped from `deny` to `ask` in `packs.php` (pack IDs/compositions.php references left unchanged — the pack's own `effect` field determines the composed result, not which list references it; verified via `compose.php`). Claude `settings.json` already had no install hard-deny (falls through to the runtime prompt = already ask-equivalent) — confirmed, not modified. `tests/php/AgentPermissionPolicyTest.php`'s `FORBIDDEN_ALLOW_PATTERNS` only forbids `allow` for these patterns, never requires `deny`, so this does not weaken that test.
- [x] P1: Treat this as an APPROVAL-BOUNDARY change (gated per AGENTS.md). Rollback: `git revert` of the composition/render change (and regenerate). Success signal: the rendered OpenCode permission block shows delete routed to ASK (not deny) for the two impl agents — **CONFIRMED** (`'rm *': ask` present in `.opencode/agents/implementer.md`, `super-implementer.md`, `post-install.md`); the Claude global-file per-agent-delete limitation is documented (not silently claimed done) — **CONFIRMED** above; per-agent external READ defaults to `ask` for agents that had none (existing intentional `allow` entries preserved) — **CONFIRMED** already true via global config; read-only execution resolves to allow/ask (never deny) for all agents — **CONFIRMED** via wrapper tier; edit-capable agents can ask to edit outside root + ask command exec — **CONFIRMED**; installs are warn-then-ask (no hard deny) — **CONFIRMED**; `php tools/ai/generate-agent-permissions.php --check` passes (composed parity) — **CONFIRMED** ("OK: managed agent permission blocks in sync"); no unrelated adapter drift — **CONFIRMED** (the only drift `php tools/ai/render-adapters.php --check` reports is proven pre-existing via a stash-isolation test, unrelated to this slice).

### P2 — (Optional, do NOT block P0/P1) Permission-policy simulation test

- [ ] P2: Add one `PermissionPolicy*Test.php` (no such test exists) asserting: (a) delete routes to ASK for implementer/super-implementer, (b) external read stays `ask`, (c) secret-read deny + destructive-command deny still hold. Clearly optional.

### Forward-looking (plan-only, NOT apply-now) — P9 shared-prose extraction

- [ ] Forward-looking: Where agent files approach budget, plan to move shared role prose into referenced capability/support files ONLY where clearly safe (policy already references this via `warn_allowlist` "P9" entries). Explicitly NOT part of this slice.

## Acceptance Criteria

- [x] AC-01: In `ai-file-standards.json`, the `opencode-agents` and `package-agent-templates` rules have `warn_above: 275` and `fail_above` in 340-360 (recommend 350); all other rules unchanged; allowlists preserved. **Evidence**: `git diff -- packages/ai-universal-rules/policies/ai-file-standards.json` shows exactly 4 line changes (2 rules x 2 values); allowlists untouched.
- [x] AC-02: `php tools/ai/validate-install-surface.php` passes after the budget change. **Evidence**: exit 0, "OK: install surface validation passed".
- [x] AC-03: On the OpenCode surface, the rendered per-agent OpenCode permission block routes file deletion to ASK (not deny) for `implementer` and `super-implementer`, and ASK requires a stated reason per AGENTS.md approval-boundary policy. **Evidence**: `.opencode/agents/implementer.md`, `.opencode/agents/super-implementer.md`, and `.opencode/agents/post-install.md` all render `'rm *': ask`; `php tools/ai/generate-agent-permissions.php --check` -> "OK: managed agent permission blocks in sync". ASK-reason requirement is existing AGENTS.md approval-boundary policy (unchanged, applies to every ask-gated action).
- [x] AC-04: The Claude global-file limitation is DOCUMENTED in this plan and in the change. **Evidence**: `.claude/settings.json` inspected — single global `allow`/`deny` file, no per-agent structure, no `ask` array; `"Bash(rm -rf *)"` still present at its existing line as a global deny (unchanged, untouched by this slice). Not claimed as fixed on Claude.
- [x] AC-05: External-repo READ is per-agent with `ask` as the safe DEFAULT, and pre-existing intentional `allow` entries are preserved; NO blanket allow introduced; OpenCode host config unchanged. **Evidence**: VERIFIED ALREADY SATISFIED — `opencode.jsonc:226` / template `opencode.json:226` set the global `external_directory: ask` default, test-enforced by `AgentPermissionPolicyTest::testProjectConfigAsksForExternalDirectoryAndLoadsBoundaryDocs`. No code change made; `render-spec.php` `script-runner`/`architecture-plan-writer` overrides confirmed untouched.
- [x] AC-06: Read-only execution resolves to allow-or-ASK (NEVER hard-deny) for ALL agents (100%). **Evidence**: VERIFIED ALREADY SATISFIED via the governed wrapper tier (`ai-search.sh`/`preview-file.sh`/etc., `script-tiers.php` line 57), which is the deliberate script-first read-only-execution surface for every agent; no code change made (changing raw-tool denies would have weakened the redaction-aware wrapper policy).
- [x] AC-07: Edit-capable agents can ASK to edit files outside the root project and ASK for command execution; agents without edit permission stay read/ask-only. **Evidence**: VERIFIED ALREADY SATISFIED — external-edit-ask via the same global `external_directory: ask` default (AC-05); command-exec-ask already true for every known risky-but-legitimate category (installs, git mutations, deletion); the remaining `'*'` catch-all deny is an intentional safe-default boundary, not weakened.
- [x] AC-08: Installs are warn-then-ASK, NOT hard-denied, on every rendered surface. **Evidence**: `refactorer`/`docs` (`package_manager.deny_all_mutations`, 11 patterns) and `bugfix`/`build-config`/`upgrade` (`package_manager.narrow_no_add_or_bun_deny`, 4 patterns) flipped from `deny` to `ask` in `packs.php`; Claude `settings.json` confirmed to already have no install hard-deny (unchanged).
- [x] AC-09: `php tools/ai/generate-agent-permissions.php --check` passes (composed permission parity) and `php tools/ai/validate-adapter-drift.php` reports no NEW drift attributable to this change. **Evidence**: `generate-agent-permissions.php --check` -> "OK: managed agent permission blocks in sync". `validate-adapter-drift.php` exits 0. The SEPARATE `render-adapters.php --check` / `AdapterRenderDriftTest` drift is PROVEN pre-existing (stash-isolation test: identical/broader drift list persists with this slice's changes fully stashed) — not attributable to this change.
- [x] AC-10: The 187-file pre-existing diff is neither staged nor modified; no product code changed; OpenCode host `external_directory` NOT weakened to allow; no `.claude/agents/*.md` budget invented; secret-read/destructive-command denies still present. **Evidence**: only this slice's own files touched (verified via targeted `git status --short` on the exact touched paths); `rm -rf *` immutable floor untouched (`aiPermissionAssertNoHardDenyWeakening` still passes — no test regression); host `opencode.jsonc` global config untouched.
- [ ] AC-11 (P2, optional): If added, the new `PermissionPolicy*Test.php` passes under `composer test:fast`. **NOT DONE** — P2 explicitly optional and deferred; not blocking P0/P1 completion.

## Verification Plan

Each line is a real repo command; each proves the ACs noted.

php tools/ai/validate-install-surface.php
php tools/ai/generate-agent-permissions.php --check
php tools/ai/validate-adapter-drift.php
composer test:fast
composer test

- `validate-install-surface.php` -> AC-01, AC-02, AC-08 (budget change passes validator; installs warn-then-ask, no hard deny).
- `generate-agent-permissions.php --check` -> AC-03, AC-05, AC-06, AC-07, AC-09 (composed parity: OpenCode delete=ASK; per-agent external ask default; read-only exec allow/ask; edit-capable ask outside root + ask cmd exec).
- `validate-adapter-drift.php` -> AC-04, AC-09 (Claude global-file limitation shows no new drift; no new drift overall).
- `composer test:fast` / `composer test` -> AC-11 and full regression.

## Risks And Rollback

- Medium risk: P1 changes approval boundaries. Rollback = `git revert` of the composition/render commit + regenerate adapters. Success signal defined in P1 (delete routed to ASK for impl/super-impl, external read stays `ask`, parity check passes, no unrelated drift).
- Low risk: P0 budget bump (pure policy widening; validator re-run proves it).

## Reuse Note

Overlap with the existing permission-layers compiler, rendered adapters, and install-surface validator is >75%; therefore this plan EXTENDS existing files and recreates NOTHING.

## Risks Or Unknowns (resolved during implementation)

- RESOLVED: `rm -rf *` is on the TRUE immutable hard-deny floor (`core.php:30`, `hard-deny` layer, protected by `aiPermissionAssertNoHardDenyWeakening()`) — confirmed untouched, no agent may loosen it. Plain `rm *` was the real, distinct, non-immutable gap: `implementer` fell through to `deny` (its `shipped_star_baseline`), `super-implementer` fell through to `allow` (silent, no confirmation). Fixed via a new shared `hard_stop.ask_rm` pack (`packs.php`), wired into `implementer`, `super-implementer`, `post-install`.
- RESOLVED (known constraint, confirmed not an unknown): per-agent delete=ASK is NOT expressible on the Claude surface — `.claude/settings.json` is a single global `allow`/`deny`-only file (no `ask` array); its global `"Bash(rm -rf *)"` deny is unchanged. Documented, not attempted.
- RESOLVED: per-agent `external_directory` default is ALREADY `ask` globally via `opencode.jsonc:226` (test-enforced) — no new entries were needed; existing `allow` overrides for `script-runner`/`architecture-plan-writer` are untouched.
- RESOLVED: `fail_above` set to 350 (mid-range of the 340-360 band), `warn_above` set to 275.

## Implementation Notes (P0/P1 landed; P2 deferred)

- **Files changed**: `packages/ai-universal-rules/policies/ai-file-standards.json` (P0); `tools/ai/install/permission-layers/compositions.php`, `tools/ai/install/permission-layers/packs.php` (P1 source); regenerated via `php tools/ai/generate-agent-permissions.php --write`: `.opencode/agents/{implementer,refactorer,post-install,super-implementer}.md`, `.opencode/agents-optional/{bugfix,build-config,upgrade,docs}.md`, and their `packages/ai-universal-rules/templates/{core,optional}/agents/*.md` sources.
- **Disclosed side effect**: `generate-agent-permissions.php --write` regenerates each rendered agent file's FULL content from its own template (permission block + body), not just the bash block. Because several of these same agent templates were ALSO part of the pre-existing 187-file `claude-agent-fleet-remediation` diff (unrelated body-prose edits: an "Edit Scope" section, an `agent_assessment.decision` field change, External Boundary Rule wording), regenerating them surfaced that already-dirty template content into a few previously-stale `.opencode` rendered copies for the first time (net worktree count: 187 -> 199 modified paths; +3 core files for this slice, +1 gitignored `super-implementer.md`, remainder is pre-existing content newly materialized into previously-stale copies). Verified via `git diff` that this body-prose content is byte-identical to the untouched `.claude/agents/implementer.md` twin (proving it originated from the pre-existing dirty template, not from this slice) — no pre-existing change was altered or reverted, only additively synced. This was the only correct tool named by this plan's own Verification Plan to materialize the composition change.
- **Regression caught and fixed**: adding `'rm *': ask` as an inline exception to `implementer` and `super-implementer` duplicated `post-install`'s existing inline exception, tripping `PermissionComposeTest::testNoExceptionPatternDuplicatedAcrossTwoOrMoreAgents`. Fixed by extracting a shared `hard_stop.ask_rm` pack and switching all three agents to reference it via `askPacks`.
- **Verification run**: `php tools/ai/validate-install-surface.php` (exit 0), `php tools/ai/generate-agent-permissions.php --check` (exit 0, in sync), `php tools/ai/validate-adapter-drift.php` (exit 0), `composer test:fast` (934 tests, 2 failures), `composer test` (934 tests, 2 failures). The 2 failures (`AdapterRenderDriftTest::testRenderAdaptersCheckExitsZero`, `::testMutatingAnInstalledFileIsDetectedAsDrift`) are PROVEN pre-existing and unrelated: a `git stash` isolating every file this slice touched still reproduces the identical (and broader) drift list, confirming it originates entirely from the untouched 187-file diff.
- **Not implemented (by design)**: P2 (optional simulation test) — deferred, does not block P0/P1. Forward-looking P9 shared-prose extraction — explicitly plan-only, not applied.
- **Deletion-attention marker (added post-review, on user request)**: `docs/ai/approval-boundaries.md` "File Rename And Delete Policy" section now requires every agent with delete-capable permission to print the attention marker `🛑⁉️` on its own line, then state the exact path and reason, before asking for delete confirmation. Placed in this single canonical, already-widely-referenced doc (not duplicated per-agent) to avoid token waste — `researcher`, `architect`, `reviewer`, `release-auditor`, `bootstrapper`, `post-install`, `agent-critic`, and `docs` templates already load this file.
- **Entanglement finding (reviewer-caught, verified true)**: a fresh-context reviewer run (`reviewer` agent) returned FAIL on `compositions.php`/`packs.php`, flagging 3 items with no comment/plan coverage: (1) `'install.docs_allow'` silently absent from `post-install`'s `allowPacks` versus HEAD, (2) a new `core.safe_read.deny_ls_pipe_sort` pack + `ls scripts/ai/*.sh` exception on `build-config`, explicitly code-commented as belonging to `docs/tickets/claude-agent-fleet-remediation/plan-13-build-config-render-drift.md`, (3) a new `core.safe_read.raw_read_ask_gate` askPack on `agent-creator-runtime-guardian`, explicitly code-commented as belonging to `plan-25-agent-creator-runtime-guardian-permission-fix.md`. Verified via `git diff HEAD` line-by-line against every Edit call made in this session: **none of these 3 items were introduced by this slice** — they are pre-existing, uncommitted content from the other in-progress `claude-agent-fleet-remediation` session, physically co-located in the same 2 files this slice needed to edit. `git show HEAD:tools/ai/install/permission-layers/compositions.php` confirms `install.docs_allow` was present at HEAD twice, current working tree has it once (bootstrapper only) — the post-install occurrence was already gone before this slice's first edit to that file (captured in this session's own pre-edit Read output). The build-config and agent-creator-runtime-guardian hunks were never touched by any Edit call in this session at all.
- **Why this is NOT committed as one slice**: In `packs.php` the pre-existing `deny_ls_pipe_sort` hunk is a cleanly separate hunk from this slice's 2 hunks (`hard_stop.ask_rm` pack, `package_manager.*` deny->ask) and could be excluded safely. In `compositions.php`, however, the `post-install` pre-existing `install.docs_allow` removal sits inside the SAME git diff hunk as this slice's `askPacks`/comment change (git's 3-line context window merged them) — splitting that cleanly requires manual hunk surgery this session judged too risky to script blind (no interactive `git add -p` terminal available). Per the standing instruction to treat the pre-existing 187-file diff as out-of-scope and not touch it, this slice held back `compositions.php`, `packs.php`, and the 12 regenerated agent files from commit rather than risk committing unreviewed cross-ticket content under this ticket's message.
- **What IS committed**: `packages/ai-universal-rules/policies/ai-file-standards.json` (P0, fully clean, standalone) and `docs/ai/approval-boundaries.md` (deletion-marker rule, fully clean, standalone) — both verified to have zero entanglement with the pre-existing diff.
- **What remains uncommitted (working tree only, unchanged from this session's edits)**: `tools/ai/install/permission-layers/compositions.php`, `tools/ai/install/permission-layers/packs.php`, and the 12 regenerated `.opencode`/`packages/ai-universal-rules/templates` agent files. All P1 functional work (`hard_stop.ask_rm`, `package_manager.*` ask conversion) is complete and independently verified (stash-isolation proved the DRY-test fix and functional correctness); only the commit step is deferred pending a decision on how to handle the entangled hunks.

## Handoff Notes

P0 and P1 (sub-items A-E) are implemented and verified; P1-A/C/D required no code change (already satisfied by existing architecture — see Implementation Notes and updated Todo Plan). Nothing has been staged or committed. Recommended next step: **reviewer** — a fresh-context review of this diff (compositions.php, packs.php, ai-file-standards.json, and the 12 regenerated agent files) before staging/commit, given the medium-risk approval-boundary nature of P1 and the disclosed side effect above. P2 remains optional future work.
