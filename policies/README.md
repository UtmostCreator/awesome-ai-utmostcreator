# Policies

This directory contains policy instances used by scripts, hooks, and AI workflow automation.

Schemas that define policy formats live in `.schemas/`.

Executable logic lives in `scripts/`.
